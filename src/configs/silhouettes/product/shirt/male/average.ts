import { GENDER, SILHOUETTES_SIZE, type ISilhouettesConfig } from "@/types"

const config: ISilhouettesConfig = {
  type: "product",
  size: SILHOUETTES_SIZE.AVERAGE,
  gender: GENDER.MALE
}

export default config